apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "chart.fullname" . }}-dnszone-admin-role
  labels:
  {{- include "chart.labels" . | nindent 4 }}
rules:
- apiGroups:
  - dns.contentways.org
  resources:
  - dnszones
  verbs:
  - '*'
- apiGroups:
  - dns.contentways.org
  resources:
  - dnszones/status
  verbs:
  - get
